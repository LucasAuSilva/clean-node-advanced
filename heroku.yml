setup:
  addons:
  - plan: heroku-postgresql
    as: DATABASE
  config:
    env_file: .env
build:
  docker:
    web: Dockerfile
release:
  command:
    - npm run migrate:prod
  image: web
run:
  web: npm start
